import { types } from "../types/types";

export const uiOpenModal = () => ({
    type: types.uiOpenModal
});

export const uiCloseModal = () => ({ type: types.uiCloseModal });

export const uiOpenModalSearch = () => ({
    type: types.uiOpenModalSearch
});

export const uiCloseModalSearch = () => ({ type: types.uiCloseModalSearch });

export const uiOpenModalAutos = () => ({
    type: types.uiOpenModalAutos
});

export const uiCloseModalAutos = () => ({ type: types.uiCloseModalAutos });
